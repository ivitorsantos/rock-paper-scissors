export default function RenderComputer(number) {
    if (number === 1) {
        return 'Pedra'
    } else if (number === 2) {
        return 'Papel'
    } else if (number === 3) {
        return 'Tesoura'
    }
}
